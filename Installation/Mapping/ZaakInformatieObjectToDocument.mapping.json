{
  "title": "ZaakInformatieObject to Document",
  "description": "Mapping for ZaakInformatieObject to Document",
  "$id": "https://commongateway.nl/mapping/ZaakInformatieObjectToDocument.mapping.json",
  "$schema": "https://docs.commongateway.nl/schemas/Mapping.schema.json",
  "version": "0.0.1",
  "passTrough": true,
  "mapping": {
    "omschrijving": "{{ type|upper|replace({'_', ' '}}",
    "omschrijvingGeneriek": "type",
    "beginGeldigheid": "{{ 'now'|date('Y-m-d') }}",
    "beginObject": "{{ 'now'|date('Y-m-d') }}"
  },
  "cast": {
    "specificatie.waardenverzameling": "unsetIfValue==enum"
  }
}
